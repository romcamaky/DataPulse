## Test Report (Phase 5)

This file was added to verify the `Reports` page:

- It should appear in the list (sorted by filename descending).
- It should render inside the expander via `st.markdown()`.
- It should download correctly via `st.download_button()`.

